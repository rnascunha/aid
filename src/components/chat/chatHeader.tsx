import { Typography } from "@mui/material";

export function ChatHeader() {
  return (
    <Typography
      variant="h3"
      component="h1"
      sx={{
        fontSize: "20px",
        fontWeight: "lg",
        mr: "auto",
      }}
    >
      AIs
    </Typography>
  );
}
