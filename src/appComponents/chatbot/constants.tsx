import { ChatbotData } from "./types";

export const app_name = "chatbot";
export const adk_api_events = "/api/adk/events";

export const defaultChatbotData: ChatbotData = {
  chats: {},
  sessions: [],
};
